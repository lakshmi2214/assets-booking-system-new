{
    "name": "asset-booking-system",
    "version": "1.0.0",
    "description": "Root package for Asset Booking System",
    "scripts": {
        "start": "npm start --prefix frontend",
        "frontend": "npm start --prefix frontend",
        "backend": "cd backend && python manage.py runserver 8050",
        "dev": "npx concurrently \"npm run backend\" \"npm run frontend\"",
        "install-frontend": "npm install --prefix frontend",
        "install-backend": "pip install -r backend/requirements.txt",
        "install-all": "npm run install-frontend && npm run install-backend",
        "sync": "python sync_db_to_mock.py"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}